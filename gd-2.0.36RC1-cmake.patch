--- origsrc/gd-2.0.36RC1/CMakeLists.txt	2007-11-27 09:30:34.000000000 +0100
+++ src/gd-2.0.36RC1/CMakeLists.txt	2014-12-08 14:14:43.078727900 +0100
@@ -74,9 +74,9 @@ else (USE_EXT_GD)
 	FIND_PACKAGE(PNG REQUIRED)
 	FIND_PACKAGE(ZLIB)
 	FIND_PACKAGE(JPEG)
-IF (NOT WIN32)
+IF (UNIX)
 	FIND_PACKAGE(PTHREAD)
-ENDIF (NOT WIN32)
+ENDIF (UNIX)
 	FIND_PACKAGE(Freetype)
 	FIND_PACKAGE(XPM)
 	FIND_PACKAGE(Fontconfig)
@@ -160,18 +160,18 @@ ENDIF (NOT WIN32)
 
 	set(BUILD_SHARED_LIBS On)
 
-	if (WIN32)
+	if (WIN32 AND NOT CYGWIN)
 		SET(GD_LIB bgd)
 		ADD_DEFINITIONS( -DWIN32 -D_WIN32 -DMSWIN32 -DBGDWIN32)
-	else(WIN32)
+	else(WIN32 AND NOT CYGWIN)
 		SET(GD_LIB gd)
-	endif(WIN32)
+	endif(WIN32 AND NOT CYGWIN)
 
 	add_library(${GD_LIB} ${LIBGD_SRC_FILES})
 
-	if (WIN32)
+	if (WIN32 AND NOT CYGWIN)
 		SET_TARGET_PROPERTIES(${GD_LIB} PROPERTIES LINK_FLAGS "/NODEFAULTLIB:msvcrt.lib")
-	ENDIF(WIN32)
+	ENDIF(WIN32 AND NOT CYGWIN)
 
 	target_link_libraries(${GD_LIB} ${FREETYPE_LIBRARIES} ${PNG_LIBRARIES} ${JPEG_LIBRARIES} ${XPM_LIBRARIES} ${FONTCONFIG_LIBRARIES})
 

